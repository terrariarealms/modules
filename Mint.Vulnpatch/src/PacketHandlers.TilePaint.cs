using Mint.Network.Incoming;
using Mint.Server;

namespace Mint.Vulnpatch;

internal static partial class PacketHandlers
{
    private static void OnTilePaint(Player player, IncomingPacket packet, ref bool ignore)
    {
    }
}